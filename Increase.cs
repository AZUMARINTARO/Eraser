using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Increase : MonoBehaviour
{
    /*float delta = 0;
    float span = 3f;
    public GameObject ball;
    void Update()
    {
        delta += Time.deltaTime;
        if (delta>span&&transform.position.x > Camera.main.ViewportToWorldPoint(new Vector2(1, 0)).x + (19f))
        {
            delta = 0;
            GameObject inc = Instantiate(ball);
            inc.transform.position = new Vector3(35, 7.2f, 0);
        }
    }*/
}